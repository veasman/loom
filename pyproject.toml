[project]
name = "loom"
version = "0.1.0"
description = "Desktop theme manager for vwm, kitty, rofi, tmux, nvim, dunst, picom, fzf"
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0.0"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
